apiVersion: v1
kind: Service
metadata:
  name: mereka-backend
  labels:
    {{- include "mereka-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mereka-backend.selectorLabels" . | nindent 4 }}
