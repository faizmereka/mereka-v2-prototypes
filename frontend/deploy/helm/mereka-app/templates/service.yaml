apiVersion: v1
kind: Service
metadata:
  name: mereka-app
  labels:
    {{- include "mereka-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mereka-app.selectorLabels" . | nindent 4 }}
