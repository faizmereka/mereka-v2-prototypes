apiVersion: v1
kind: Service
metadata:
  name: mereka-admin
  labels:
    {{- include "mereka-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mereka-admin.selectorLabels" . | nindent 4 }}
