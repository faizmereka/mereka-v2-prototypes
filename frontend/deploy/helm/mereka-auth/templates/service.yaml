apiVersion: v1
kind: Service
metadata:
  name: mereka-auth
  labels:
    {{- include "mereka-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mereka-auth.selectorLabels" . | nindent 4 }}
